  <h1><%= @task.title %></h1>
  <br>
  <h2><%= @task.details %></h2>
<br>
<h3> <%= "this task is completed" if @task.completed == true %></h3>

<%= link_to "Back to list", tasks_path %>
<%= link_to "Edit this Task", edit_path %>
<br>
<%= link_to "DELETE #{@task.title}", task_path(@task), method: :delete, data: { confirm: "Are U sure?"} %>


